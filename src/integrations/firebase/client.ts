// Legacy stub — kept so old imports compile. All data is now in Lovable Cloud.
export const rtdb = null as never;
export const db = null as never;
export const auth = null as never;
export const googleProvider = null as never;
