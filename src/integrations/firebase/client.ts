// Legacy shim — firebase is no longer used. These exports exist only to satisfy
// any straggling imports during the migration. Do not add new usages.
export const auth = {} as never;
export const db = {} as never;
export const rtdb = {} as never;
export const googleProvider = {} as never;
export default {} as never;
